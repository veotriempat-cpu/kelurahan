// DEBUG MODE
// F12, Developer Tools, right click, View Source, and browser shortcuts are intentionally enabled
// so the application can be debugged in the browser console.
// Re-enable client-side deterrence only after all click/navigation issues are resolved.
export {};
